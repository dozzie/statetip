#!/usr/bin/make -f

%:
	dh $@

override_dh_installinit:
	dh_installinit --name=statetip

override_dh_auto_build:
	make all
	make doc

override_dh_auto_install:
	make install DESTDIR=$(CURDIR)/debian/tmp
	make install-doc DESTDIR=$(CURDIR)/debian/tmp
